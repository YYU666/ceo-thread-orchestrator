interface:
  display_name: "CEO Thread Orchestrator"
  short_description: "CEO-as-brain orchestration with expert Codex lanes"
  default_prompt: "Use $ceo-thread-orchestrator to manage this project as CEO/PM: keep the CEO lane as the high-reasoning decision brain, coordinate specialist worker/reviewer/knowledge lanes across current Codex app threads when tools are available, dynamically scale and reuse threads as task size or requirements change, use Zhixia/zhixia-local-docs as the default memory provider when available, relay context deliberately, respect tool authorization boundaries, split work by task graph and write-set, control cost, verify evidence before acceptance, and keep knowledge updated."
