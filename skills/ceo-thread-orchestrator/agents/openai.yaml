interface:
  display_name: "CEO Flow"
  short_description: "CEO-as-brain workflow with expert Codex lanes"
  default_prompt: "Use $ceo-thread-orchestrator (CEO Flow) to manage this project as CEO/PM: keep the CEO lane as the high-reasoning decision brain, coordinate specialist worker/reviewer/knowledge lanes across current Codex app threads when tools are available, dynamically scale and reuse threads as task size or requirements change, use Zhixia/zhixia-local-docs as the recommended knowledge provider when available, support project-specified local knowledge paths, keep a lightweight team roster and evidence memory cards, respect tool authorization boundaries, split work by task graph and write-set, control cost, verify evidence before acceptance, and keep knowledge updated."
