# Tool Boundary

Use only `read`, `apply_patch`, `exec`, and `process`, further restricted by the current task. Keep every result within the per-result cap and the whole run within the cumulative tool-result budget.
